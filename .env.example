# This file contains environment variables that are required for the application to run.
# It should be committed to version control to serve as a template for other developers.
# NEVER commit sensitive information (like actual API keys or secrets) to this file.
# Instead, create a local .env file (which is git-ignored) and populate it with your actual values.

# --- Core Authentication Variables (Auth.js) ---

# A random string used to hash tokens, sign cookies and generate cryptographic keys.
# You can generate one with `openssl rand -base64 32` or at https://generate-secret.vercel.app/32
# This MUST be kept secret and should be at least 32 characters long.
AUTH_SECRET="YOUR_AUTH_SECRET"

# --- Auth0 Provider Credentials ---

# Your Auth0 application's Client ID.
# Found in your Auth0 dashboard under Applications -> Your Application -> Settings.
AUTH0_CLIENT_ID="YOUR_AUTH0_CLIENT_ID"

# Your Auth0 application's Client Secret.
# Found in your Auth0 dashboard under Applications -> Your Application -> Settings.
# This MUST be kept secret.
AUTH0_CLIENT_SECRET="YOUR_AUTH0_CLIENT_SECRET"

# Your Auth0 tenant's Issuer Base URL.
# This is typically in the format `https://YOUR_AUTH0_DOMAIN/`.
# Found in your Auth0 dashboard under Applications -> Your Application -> Settings -> Domain.
AUTH0_ISSUER="https://YOUR_AUTH0_DOMAIN/"

# The unique identifier for the API you created in your Auth0 dashboard.
# This is used to specify which 'audience' the access token is for.
# Found in your Auth0 dashboard under Applications -> APIs -> Your API -> Settings.
AUTH0_AUDIENCE="https://api.studypuck.app"

# --- Database Configuration (Neon Postgres) ---

# Neon Postgres database connection string with pgvector extension.
# Replace with your actual Neon database connection string.
# Format: postgresql://username:password@host:port/database?sslmode=require&channel_binding=require
# Get this from your Neon dashboard under Connection Details.
DATABASE_URL="postgresql://example_user:example_password_123@ep-example-region-abc123.c-1.us-west-1.aws.neon.tech/example_db?sslmode=require&channel_binding=require"
